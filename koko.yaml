log:
  level: debug
database:
  dialect: sqlite3
  query_timeout: 300ms
  sqlite:
    #in_memory: true
    filename: test.db
  postgres:
    hostname: localhost
    port: 5432
    db_name: koko
    user: koko
    password: koko
control_server:
  tls_cert_path: cluster.crt
  tls_key_path: cluster.key
