package info

var (
	// VERSION is the current version.
	// This is substituted by git tag during the build process.
	VERSION = "dev"

	// COMMIT is the short hash of the source tree.
	// This is substituted by Git commit hash  during the build process.
	COMMIT = "unknown"
)
